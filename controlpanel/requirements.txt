# G1 Interactive — control panel dependencies (install on the host that runs the panel)
#   python -m pip install -r controlpanel/requirements.txt
# Then run from the project root:  python -m controlpanel

fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0   # ASGI server + websockets + uvloop
python-multipart>=0.0.9        # script upload (multipart form parsing)
