"""G1 Interactive — web control panel (FastAPI).

Manages the voice pipeline: start/stop/restart, live log console, knowledge base,
instructions/persona, environment (keys + voice id), diagnostic scripts, and a live
conversation transcript + rough cost view.

Run from the project root:  python -m controlpanel   (binds PANEL_HOST:PANEL_PORT)
"""
__version__ = "1.1.0-dev"
